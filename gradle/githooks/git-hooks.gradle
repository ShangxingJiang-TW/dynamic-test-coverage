task installGitHooks(type: Copy) {
    from new File(rootProject.rootDir, 'gradle/githooks/pre-push')
    into {
        new File(rootProject.rootDir, '.git/hooks')
    }
    fileMode 0755
}

compileJava.dependsOn installGitHooks
